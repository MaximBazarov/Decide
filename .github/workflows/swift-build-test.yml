name: Unit Tests

on:
  pull_request:
    branches: 
    - main
    paths-ignore:
    - 'Sources/Decide.docc/**'

jobs:
  build:
    runs-on: macos-13
    steps:
    - uses: swift-actions/setup-swift@v1
      with:
        swift-version: "5.7"
    - uses: actions/checkout@v3
    - name: Test
      run: swift test -v
